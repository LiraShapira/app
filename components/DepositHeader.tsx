import { Text, View } from 'react-native';

export default function DepositHeader() {
  return (
    <View
      style={{
        height: 40,
        backgroundColor: 'white',
      }}
    >
      <Text>Deposit Header</Text>
    </View>
  );
}
