{
  "baseUrl": ".",
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
  },
  "paths": {
    "components/*": ["components/*"],
    "constants/*": ["constants/*"]
  },
  "include": [
    "src/**/*.ts",  // Adjust the pattern based on your project structure
    "jest.config.cjs"
  ]
}
